# Cached raw data (large, regenerable)
data/cache/

# Confirmed classification queue (contains PD text, not for public repo)
data/training/confirmed_queue.csv

# Office temp/lock files
~$*

# H2O model artifacts — UN-IGNORED so inheriting team gets the models
# (H2O can't run on Streamlit Cloud free tier, but works locally)
# models/

# CQM sessions
cqm-sessions/

# ── Secrets ──────────────────────────────────────────────────
.env
.env.local
.env.*
!.env.example
*.env
.streamlit/secrets.toml
**/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# R
.Rhistory
.RData

# H2O runtime
h2o_logs/

# Professor's class materials (not for repo)
_temp holding/

# Local process artifacts and history — stashed out of the public repo
# on 2026-04-14 to present a polished final deliverable on GitHub.
# Contents are retained on the Owner's local filesystem only.
_local_archive/

# Rendered HTML build artifacts (regenerable from .md source)
QUICK_START.html
docs/DEPLOYMENT.html

# Downloaded Streamlit Cloud build logs (ephemeral debug artifacts)
logs-*.txt

# Internal-audience presentation materials — targeted at in-person W&M
# audiences (Big Data class, poster session), intentionally keep client
# context. NOT for public GitHub. Demo_Photos/ stays tracked because
# the screenshots show only the team-branded Streamlit UI.
ppt/*.pptx
ppt/*.html
