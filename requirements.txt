# requirements.txt — Streamlit Community Cloud deployment
# Canonical dev env is environment-local.yml; this file is the Streamlit Cloud build source.
# See docs/DEPLOYMENT.md for the deployment runbook.
#
# Scope: this file lists ONLY the packages the deployed Streamlit app imports
# (streamlit_app.py + feature_extraction.py + ml_classifier.py). Notebook-only
# deps (shap, requests, beautifulsoup4, sqlalchemy, pymysql, lxml) live in
# environment.yml and are NOT installed on Streamlit Cloud — keeping the
# cloud build fast and under the Streamlit Cloud memory cap.

# --- Streamlit + UI ---
streamlit>=1.32

# --- LLM (Gemini via LangChain agent pipeline) ---
google-generativeai>=0.5
python-dotenv>=1.0
langchain>=0.3
langgraph>=0.2
langchain-google-genai>=2.0

# --- Data handling ---
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# --- ML (sklearn for Cloud deploy; H2O for local/notebook dev) ---
# H2O is our primary classifier in local and notebook environments (higher
# accuracy on the Workday features set), but the h2o Python package + JVM
# footprint exceeds Streamlit Community Cloud's 1 GB memory tier. The Cloud
# build therefore ships sklearn only; ml_classifier.py transparently falls
# back to the sklearn GBM when H2O is unavailable (see ml_classifier.py:60).
# FUTURE ROLLOUT: if we migrate to Streamlit in Snowflake or any paid tier
# with >=2 GB RAM, re-enable H2O by uncommenting the line below.
scikit-learn>=1.3
# h2o>=3.46  # disabled on Streamlit Cloud — over 1 GB memory cap; see note above

# --- Document export ---
python-docx>=1.1
